// Placeholder for Phase 2: Extract dominant colors from images
export const extractColors = async (_imageUrl: string): Promise<string[]> => {
  return new Promise((resolve) => {
    resolve(['#000000', '#ffffff']);
  });
};
